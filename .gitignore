# Reference + working files are regenerated by the pipeline
reference/
work/

# Remote-index caches that htslib writes into the working dir during streaming
*.bam.bai
HG002_GRCh38_*_benchmark.vcf.gz.tbi
HG002.GRCh38.300x.bam*

# Quarto / R scratch
.quarto/
_freeze/
.Rhistory

# Root-level deliverable render (the servable copy lives in docs/)
/deliverable.pdf
deliverable.typ

# Large regenerated outputs (kept only locally for the optional IGV view)
results/*.bam
results/*.bai
results/*.vcf.gz
results/*.vcf.gz.tbi

# Note: data/ (small committed HG002 inputs) is intentionally tracked.

# OS
.DS_Store
Thumbs.db

/.quarto/
**/*.quarto_ipynb
